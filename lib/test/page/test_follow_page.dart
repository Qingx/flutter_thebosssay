import 'package:flutter/material.dart';

class TestFollowPage extends StatelessWidget {
  const TestFollowPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
